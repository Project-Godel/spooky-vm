extern void printInt(int i)

void main() {
  for (int i = 1; i <= 15; ++i) {
      if (i % 3 == 0 && i % 5 == 0) {
        printInt(12);
      } else if (i % 3 == 0) {
        printInt(1);
      } else if (i % 5 == 0) {
        printInt(2);
      } else {
        printInt(0);
      }
  }
}
