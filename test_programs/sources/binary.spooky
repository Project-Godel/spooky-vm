extern void print(int ch)

void printBinary(int n) {
    while (n != 0) {
        print(48 + n % 2);
        n = n / 2;
    }
    print(10);
}

void main() {
    printBinary(1);
    printBinary(7);
    printBinary(10);
}